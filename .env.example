# Supabase — required for auth. If both are unset, the app runs in demo mode (no auth).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Custom Icaro backend API (used by src/lib/api). Defaults to
# http://192.168.18.242:3001 if left blank.
VITE_API_BASE_URL=http://192.168.18.242:3001
